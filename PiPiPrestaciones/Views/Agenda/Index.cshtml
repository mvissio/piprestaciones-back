@model IEnumerable<PiPiPrestaciones.Models.Agenda>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FechaEvento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ImagenPreview)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ContenidoPublicacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TituloPublicacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FechaEvento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ImagenPreview)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContenidoPublicacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TituloPublicacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AgendaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AgendaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AgendaId })
        </td>
    </tr>
}

</table>
